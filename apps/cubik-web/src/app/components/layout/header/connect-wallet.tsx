"use client"
import { Button } from "@chakra-ui/react";

const ConnectWallet = () => {
    return (
        <Button variant="primary" size="xs" >Connect Wallet</Button>
    );
}

export default ConnectWallet;